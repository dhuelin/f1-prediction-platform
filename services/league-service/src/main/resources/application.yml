server:
  port: 8083

spring:
  application:
    name: league-service
  datasource:
    url: jdbc:postgresql://localhost:5432/league_db
    username: f1predict
    password: f1predict  # dev default — override via env var in production
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    locations: classpath:db/migration
  rabbitmq:
    host: localhost
    port: 5672
    username: f1predict
    password: f1predict  # dev default — override via env var in production
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

services:
  scoring-url: http://localhost:8084
